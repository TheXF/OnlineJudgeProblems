#include <bits/stdc++.h>

using namespace std;

#define se second
#define fs first
typedef pair<int,int> P;
const int maxn=1e6+3000;
int n,T;
int a[maxn];
int maxp[maxn],
int main(){
	freopen("./in","r",stdin);
	cin>>T;
	while(T--){
		scanf("%d",&n);
		for(int i=0;i<n;i++) scanf("%d",&a[i].fs),a[i].se=i;
		sort(a,a+n);

	}
	return 0;
}
